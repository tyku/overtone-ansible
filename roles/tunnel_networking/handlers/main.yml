---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart inference tunnel proxy
  ansible.builtin.systemd_service:
    name: "{{ inference_tunnel_proxy_service_name }}"
    state: restarted

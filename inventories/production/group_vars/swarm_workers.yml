---
ssh_policy_profile: worker
ssh_policy_dropin_path: /etc/ssh/sshd_config.d/00-overtone-internal.conf

# Reverse SSH tunnels are restricted to these loopback ports.
ssh_permit_listen:
  - 127.0.0.1:50052
  - 127.0.0.1:50053
  - 127.0.0.1:50054
